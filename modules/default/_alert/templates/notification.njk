{% if title %}
    <span class="thin normal medium">{{ title if titleType == 'text' else title | safe }}</span>
{% endif %}
{% if message %}
    {% if title %}<br />{% endif %}
    <span class="light bright xmedium">{{ message if messageType == 'text' else message | safe }}</span>
{% endif %}
